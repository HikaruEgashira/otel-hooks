[project]
name = "cc-tracing-hooks"
version = "0.1.0"
description = "Claude Code tracing hooks for Langfuse observability"
requires-python = ">=3.12"
dependencies = ["langfuse"]

[project.scripts]
cc-tracing-hooks = "cc_tracing_hooks.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
